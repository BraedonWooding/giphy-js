{
    "scripts": {
        "clean": "rm -rf ./dist",
        "dev": "tsc --watch",
        "build": "tsc -d --emitDeclarationOnly -declarationDir ./dist",
        "prepublish": "npm run clean && tsc -d --emitDeclarationOnly -declarationDir ./dist"
    },
    "name": "@f-giphy/js-types",
    "version": "3.1.1",
    "main": "dist/index.d.ts",
    "types": "dist/index.d.ts",
    "publishConfig": {
        "access": "public"
    },
    "files": [
        "dist/**/*"
    ],
    "license": "MIT",
    "devDependencies": {
        "typescript": "^4.0.5"
    }
}
