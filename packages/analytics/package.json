{
    "scripts": {
        "clean": "rm -rf ./dist",
        "dev": "tsc --watch",
        "prepublish": "npm run clean && tsc",
        "build": "tsc",
        "test": "jest --config ./jestconfig.js",
        "test:watch": "jest --config ./jestconfig.js --watchAll"
    },
    "name": "@f-giphy/js-analytics",
    "version": "3.1.2",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist/**/*",
        "src/**/*"
    ],
    "license": "MIT",
    "publishConfig": {
        "access": "public"
    },
    "dependencies": {
        "@f-giphy/js-types": "^3.1.1",
        "@f-giphy/js-util": "^2.3.1",
        "append-query": "^2.1.0",
        "throttle-debounce": "^2.3.0"
    },
    "devDependencies": {
        "@types/append-query": "^2.0.0",
        "@types/throttle-debounce": "2.1.0",
        "jest-fetch-mock": "^3.0.3",
        "typescript": "^4.0.5"
    }
}
