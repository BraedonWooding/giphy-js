{
    "packages": ["packages/*"],
    "command": {
        "publish": {
            "allowBranch": ["master"]
        }
    },
    "version": "independent",
    "npmClient": "yarn",
    "workspace": true
}
